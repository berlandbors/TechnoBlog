# WiFi Diagnostics Module

## Описание (Description)

Модуль диагностики WiFi и сети для браузера. Использует стандартные Web API для получения информации о сетевом соединении и проведения тестов производительности.

WiFi and network diagnostics module for the browser. Uses standard Web APIs to get network connection information and perform performance tests.

## Возможности (Features)

- **Online/Offline статус** - Определение текущего состояния подключения
- **Network Information API** - Получение детальной информации о соединении:
  - Тип соединения (WiFi, сотовая сеть, ethernet)
  - Эффективный тип сети (2G, 3G, 4G)
  - Скорость downlink (Мбит/с)
  - RTT (Round-trip time) - задержка в мс
  - Режим экономии трафика
- **Тесты производительности**:
  - Измерение задержки (ping-подобный тест)
  - Измерение скорости загрузки
- **Визуальный интерфейс**:
  - Модальное окно с результатами
  - Индикаторы качества соединения
  - Поддержка светлой и темной темы
  - Русская локализация

## Использование (Usage)

### Автоматическая инициализация

Модуль автоматически инициализируется при загрузке страницы и создает кнопку "📶 WiFi" в правом нижнем углу экрана.

### Программный доступ

Доступ к API диагностики через глобальный объект:

```javascript
// Показать модальное окно
window.wifiDiagnostics.show();

// Скрыть модальное окно
window.wifiDiagnostics.hide();

// Получить информацию о соединении
const info = window.wifiDiagnostics.getInfo();

// Запустить полную диагностику
const results = await window.wifiDiagnostics.runTests();

// Получить качество соединения
const quality = window.wifiDiagnostics.getQuality();
```

## Файлы модуля (Module Files)

- `wifiDiagnostics.js` - Основной модуль диагностики
- `wifiDiagnosticsUI.js` - Модуль пользовательского интерфейса
- `wifiDiagnostics.css` - Стили для интерфейса
- `wifiDiagnosticsInit.js` - Скрипт инициализации

## Требования (Requirements)

- Современный браузер с поддержкой:
  - Network Information API (опционально, для расширенной информации)
  - Fetch API
  - Performance API
  - ES6+

## Совместимость (Compatibility)

Модуль работает во всех современных браузерах. Network Information API поддерживается в:
- Chrome 61+
- Edge 79+
- Opera 48+
- Samsung Internet 8.0+

В браузерах без поддержки Network Information API модуль работает в базовом режиме, показывая только online/offline статус.

## Примечания (Notes)

- Тесты скорости и задержки требуют доступа к внешним ресурсам
- В режиме офлайн доступна только базовая информация о статусе
- Результаты тестов могут варьироваться в зависимости от сетевых условий

## Лицензия (License)

© Берлин Андрей
